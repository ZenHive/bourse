# Post-merge audit — 853cf81

Range audited (already merged, never reverted):

- `853cf81` fix(unified): gate parser aliases on venue evidence
- `ecf953e` harness: agent delivery — task 565 (repair the return-type resolution table)

## Scope reviewed

- **Library code** — `lib/bourse/unified.ex`, `lib/bourse/unified/read_parse.ex`,
  `lib/bourse/unified/field_maps.ex`, `lib/bourse/exchange.ex`,
  `lib/bourse/oracle_provenance/derivation.ex`,
  `lib/bourse/recorded_response_fixtures/capture.ex`.
- **Docs** — new carve register entries for binance, binanceusdm, bybit, okx
  (`853cf81`) and deribit (`ecf953e`).
- **Fixtures / manifests** — response recordings, accepted-request manifest, and the
  return-type-resolution / coverage-exclusion fixtures (data, not audited for prose).
- **Suppression file** — `.sobelow-skips` re-ordering (line-number churn only, no new
  suppressions and no buried live finding).

## Findings

1. **CHANGELOG gap (fixed).** Neither delivery commit carried a `CHANGELOG.md` entry —
   correct for a harness delivery commit (roadmap/CHANGELOG are excluded from the delivery
   diff), but the maintained `[Unreleased]` section then had no record of two substantive,
   user-facing fixes: the return-type resolution repair that unblocked 51 previously
   dead-parsing reads, and the venue-evidence gating of the newly resolvable aliases.
   Added two entries under `[Unreleased] > Fixed`.

2. **Doc formatting inconsistency (fixed).** The `C-T565a` insertion in
   `docs/authored-spec-carves/deribit.md` introduced two double-blank-line runs, where
   every other carve block in the file (and the register convention) uses a single blank
   separator. Normalized both to single blanks.

## Checked and clean (no action)

- **No dead code / orphaned references.** `ecf953e` pruned 125 entries from
  `test/fixtures/unified_read_parse_coverage_exclusions.json` (reads that now resolve); the
  file still exists and its `@exclusions_path` test reference remains valid — no orphan.
- **No debug leftovers.** No `IO.inspect`/`IO.puts`/`dbg`/`TODO`/`FIXME` in the added lib
  lines.
- **Referenced modules exist.** `Bourse.LastPrice`, `Bourse.LeverageTier`,
  `Bourse.OpenInterest`, `Bourse.MarginMode`, `Bourse.Liquidation` all present (also
  confirmed by the clean compile).
- **Naming/conventions consistent.** New alias-table entries, symbol-dict return methods,
  and the `flatten_binance_leverage_tiers` / hyperliquid open-interest annotators follow the
  established patterns in each module.

## Cold-build witness

Ran `mix deps.get && mix check.dispatch` in this intentionally un-warmed worktree.
Result: **passed (exit 0)** — precommit, `ccxt.oracle_gate` ("all modules verified",
"binary oracle exact-set ratchet passed"), lighter signer build + parser coverage 95.76%,
`ccxt.claude_check` ("mechanical claims match the tree"), `ccxt.agents_md --check`
("AGENTS.md is up to date"), `ex_dna` (no duplication), and `reach.check` (architecture +
smells OK) all green.

## Discoveries filed

None. The landed work is sound and self-consistent; no follow-up tech debt, orphaned path,
uncovered edge case, or deferred decision warranted an rmap task.

## Reviewer-rejection note

No reviewer rejections were recorded for this project, so no false-rejection assessment
applies.
