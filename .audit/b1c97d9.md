# Post-merge audit — b1c97d9

Range audited (already merged, never reverted):

- `d00b3b5` task 652 agent delivery — emulated reads forward the caller
  parameter map instead of rebuilding it from three hardcoded keys
- `099b271` task 652 reviewer fixes — pin consumed-selector stripping and
  unknown-param passthrough
- `b1c97d9` task 652 reviewer fixes — drop the stale `network` consume on
  `fetch_deposit_addresses`, add the capability-wide `until` sweep and the
  AST rebuild guard

## Scope reviewed

- **Runtime code** — `Bourse.Emulation` handlers, `@consumed_delegated_params`,
  and `delegated_params/4`. The old `list_args_to_params/1` rebuild and
  `drop_params/2` helper are gone; every nested `call_method` /
  `call_optional_method` site now starts from the incoming map.
- **Tests** — `test/bourse/emulation_test.exs` (register freshness, local
  compensation, `until` forwarding, deposit-address `network` passthrough)
  and the futures order-history `endTime` pin in
  `test/bourse/time_window_integration_test.exs`.
- **Documentation** — `[Unreleased]` and the `Bourse.Emulation` moduledoc.

## Findings

1. **`[Unreleased]` omitted the consumer-visible param-passthrough fix
   (fixed).** Callers of emulated reads (`fetch_closed_orders`,
   `fetch_ticker`, and the rest of the emulated surface) can now pass
   `until` and venue-native options through to the nested method. That
   contract change was not in the changelog. Added a Fixed entry.

2. **`Bourse.Emulation` moduledoc did not state the new forwarding
   contract (fixed).** The module is the owner of the drop register. Added
   a short paragraph so the next author does not reintroduce a hardcoded
   rebuild.

## Checked and clean

- No leftover `IO.inspect`, `dbg`, bare TODO/FIXME, or debug prints in the
  landed range.
- No stale `drop_params` / `list_args_to_params` call sites.
- The consumed-param register matches the handlers: `symbol` is rewritten
  to `symbols` or used as a local pick; `id` / `order` / `trades` /
  `network` (by-network path only) are local selectors; `network` is
  forwarded when the nested source is `fetch_deposit_addresses`.
- Client-side `filter_by_since_limit` still does not apply `until`. That
  is not a regression of this range: `until` is now forwarded to the
  nested method, and the time-window test pins `endTime` on the futures
  order-history wire. Local upper-bound filtering would be a separate
  product change, not a hygiene fix.
- `handle_fetch_currencies/4` remains public because the strategies suite
  calls it directly; this range only stopped ignoring `params`.

## Verification

- `mix deps.get && mix check.dispatch` in this intentionally un-warmed
  worktree: **passed**. Offline suite **4,014 passed / 0 failed / 288
  excluded / 1 flaky** (Lighter worker sanitization retried green — not
  introduced by this range). Oracle gate, authority check, error
  authority, `ccxt.claude_check`, `ccxt.agents_md --check`,
  `ex_dna --max-clones 0`, and `reach.check --arch --smells --strict`
  all passed.

## Cold-build witness

Ran `mix deps.get && mix check.dispatch` in the intentionally un-warmed
worktree (no copied `deps/` or `_build/` at start). First compile built
deps and the project; the dispatch gate finished green as above. This
post-merge fact did not revert, unmerge, or block the settled range.

## Discoveries filed

None. The changelog and moduledoc gaps were small enough to fix here.
The Lighter helper flake recovered on retry and sits outside this range.

## Reviewer-rejection note

No reviewer rejections were supplied for this project, so no false-rejection
assessment applies.
