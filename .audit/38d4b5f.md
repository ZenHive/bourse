# Post-merge audit — 38d4b5f

Range audited (already merged, never reverted):

- `6298f1d` / `d6d105b` task 651 caller-input error normalization and invariant sweep
- `19be7bc` task 633 Binance futures time-window request shaping
- `3bf38a0` / `0b249cf` tasks 638/645 WebSocket owner lifecycle and Lighter snapshot preservation
- `efe05ea` task 646 funding-rate market identity enforcement
- `38d4b5f` task 649 release-pinned capability surface

## Scope reviewed

- **Runtime code** — unified exception normalization, funding-rate result shaping,
  WebSocket connection-owner supervision/replacement, subscribe acknowledgement
  handling, and capability-surface comparison.
- **Authored evidence and packaging** — Binance/Binance futures/Lighter carve
  entries, authored request shapes, the packaged capability JSON, oracle-gate
  ratchet, and Hex package file inventory.
- **Tests and documentation** — caller-input class sweep, funding and time-window
  regressions, WebSocket lifecycle/snapshot tests, `[Unreleased]`, `BUGS.md`, and
  generated CLAUDE/AGENTS claims.

## Findings

1. **Consumer CHANGELOG coverage was incomplete (fixed).** Task 633 was already
   recorded, but the release-pinned capability API, reconnect-owner cleanup,
   Lighter snapshot preservation, Binance funding-market refusal, and the two
   remaining caller-input tuple conversions were absent. Added concise entries
   under `[Unreleased]`.

2. **The withdrawn capability bug ended with stale task-649 wording (fixed).**
   `BUGS.md` still said task 649 could be reduced or closed after the reporter's
   retraction. Appended the shipped outcome: the packaged surface and oracle
   ratchet now pin future release changes, while the report remains withdrawn
   because the observed block was consumer-side exchange selection.

3. **The caller-input invariant comment had stale cardinality (fixed).** The
   sweep now invokes four request-shape reasons but warned about “a fourth”
   validator. Reworded it to cover any additional validator.

## Checked and clean

- No leftover `IO.inspect`, `dbg`, bare TODO/FIXME, whitespace damage, or
  obsolete `owns_connection?` helper/reference remains in the landed range.
- CLAUDE.md and the committed generated AGENTS.md both name the new connection
  owner supervisor; the working AGENTS.md has only the harness-injected
  ephemeral preamble and was preserved outside the commit.
- Capability values are boolean/string declarations across all eleven runtime
  venues, the packaged file is covered by the package inventory test, and the
  oracle test pins a named value flip.
- Funding refusal, time-window rename/omit behavior, reconnect cleanup, owner
  crash propagation, and Lighter snapshot re-queueing all have focused positive
  and error-path tests.

## Verification

- `mix format --check-formatted test/bourse/unified_test.exs`: passed.
- `mix test.json --quiet test/bourse/unified_test.exs --output
  /tmp/audit-38d4b5f-focused.json`: **152 passed / 0 failed**.

## Cold-build witness

Ran `mix deps.get && mix check.dispatch` in the intentionally un-warmed
worktree, capturing the first run. The cold compile reached the offline suite:
**3,960 passed / 1 failed / 288 excluded**. The sole failure was
`test/mix/tasks/ccxt_agents_md_test.exs:407`: harness prepends its ephemeral
operation rules to the working `AGENTS.md`, so the committed-tree freshness test
reports `STALE`. The injected file was preserved and excluded from this audit
commit. This red post-merge fact did not revert, unmerge, or block the settled
range.

## Discoveries filed

None. Every concrete hygiene issue was small enough to fix in this audit; no
separate rmap task was warranted.

## Reviewer-rejection note

No reviewer rejections were supplied for this project, so no false-rejection
assessment applies.
