# Post-merge audit — 3ec50a8

Range audited (already merged, never reverted):

- `3ec50a8` harness: reviewer fixes — task 617 pin open-orders omit on the wire and carve the residual
- `0078265` harness: agent delivery — task 617 request-side since/until guard and OKX inclusive candle window

## Scope reviewed

- **Runtime code** — OKX `put_ohlcv_cursors/3` now sends `after = until + 1`;
  Binance spot authored request shape adds `startTime`/`endTime` on the
  emulated closed/canceled histories and `fetchOrderTrades`, and `_omit`s
  `since`/`until` on `fetchOpenOrders`.
- **Tests and goldens** — the offline request-shape allowlist, the live-probe
  only-shrink ratchet, Binance dispatch goldens, OKX authored-spec and
  request-shape pins, and the recaptured OKX history-candle accepted-request
  recordings.
- **Carve registers** — C-T617a (OKX inclusive candle window, superseding
  C-T553d's `until` translation) and C-T617b (spot residual).
- **Consumer docs** — `[Unreleased]` and BUGS.md.

## Findings

1. **CHANGELOG gap (fixed).** Task 617 changed consumer-visible request
   translation and was missing from `[Unreleased]`. Added the inclusive OKX
   candle `until` compensation and the Binance spot history rename / open-orders
   omit.

## Checked and clean

- C-T553d remains the historical append-only record (`after = until`);
  C-T617a is the current contract. No other docs still state the old mapping
  as live.
- Recaptured OKX history-candle goldens match the new cursors
  (`before = since - 1`, `after = until + 1` on a 10×1m derived window).
- No leftover `IO.inspect` / `dbg` / bare TODO, no leaked harness scaffolding,
  no stale BUGS entries for 617.
- The offline allowlist rewrite from "drop-or-reject carve" to "raw
  since/until pass through" matches what `RequestShape.apply/4` actually
  emits.

## Cold-build witness

Ran `mix deps.get && mix check.dispatch` in this intentionally un-warmed
worktree. Result: **passed (exit 0)**. Offline suite **3,878 passed / 0 failed /
282 excluded**. Oracle gate, error authority, lighter signer,
`ccxt.claude_check`, `ccxt.agents_md --check`, `ex_dna` (0 clones), and
`reach.check --arch --smells --strict` all green.

## Discoveries filed

**Task 633** — Binance USD-M and COIN-M still pass raw `since`/`until` on
open-orders and on histories whose provider contracts name `startTime`/`endTime`.
617's AC only required the spot residual; the same unread-parameter / rename
class remains on the dedicated futures venues and is now enumerated by the
new request-side allowlist.

## Reviewer-rejection note

The supplied recent rejection concerns task 551, which is not in this landed
range, so no false-rejection assessment applies.
