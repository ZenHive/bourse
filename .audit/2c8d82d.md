# Post-merge audit — 2c8d82d

Range audited (already merged, never reverted):

- `2c8d82d` / `1afbe1c` task 547 alpaca trade history (and carved transfer absence)
- `2fde2c8` task 622 client-order-id round-trip invariant
- `429a8e9` task 580 binance-family algo-book order reads
- `676797d` / `b6346a9` task 630 owned, reused, option-preserving split-host WS connections

## Scope reviewed

- **Runtime code** — alpaca trade parse/envelope, binance algo-book fan-out and
  order annotation, Deribit `label` request/response mapping, `Bourse.Trade`
  `client_order_id`, and pooled split-host WebSocket ownership.
- **Tests and evidence** — authored-slice and integration pins, accepted-request
  goldens, the client-order-id catalog invariant, time-window inventory, and
  WS host-lifecycle tests.
- **Consumer docs** — CHANGELOG `[Unreleased]`, BUGS statuses for 580 and 622,
  alpaca/deribit/global carve registers.

## Findings

1. **CHANGELOG gap (fixed).** Tasks 547, 580, and 622 are consumer-facing and
   were missing from `[Unreleased]` (630 was already recorded). Added
   `Bourse.Trade.client_order_id`, alpaca `fetch_trades` / `fetch_my_trades`
   with the carved wallet-transfer absence, algo-book read/ack/type parity,
   and the Deribit client-id round-trip.

2. **Stale BUGS statuses (fixed).** The two 2026-08-10 binanceusdm algo entries
   still said 📋 triaged after 580 landed the cancel-ack `status: "canceled"`
   synthesis and `STOP` / `STOP_MARKET` type mapping. The 2026-08-17 deribit
   `client_order_id` entry still said 🔀 triaged after 622 landed. Headers now
   say ✅ fixed and name the delivery commits.

3. **Leaked harness dispatch rule (fixed).** Task 630 committed
   `.cursor/rules/harness-operational.mdc`, which is dispatch-injected
   scaffolding, not product. Removed it and gitignored the path next to
   `.harness/`.

## Checked and clean

- Task 547's transfer/deposit/withdrawal absence is recorded as C-T547c with
  live paper-host 404 evidence; no follow-up task is warranted.
- Task 580's cancel-ack path writes `_bourse_status: "CANCELED"`, which the
  authored map emits as unified `"canceled"`. Fan-out now builds per-path
  params so algo identifiers reach the algo book.
- Task 622's catalog invariant requires both-or-neither mapping; Deribit
  order and trade field maps echo `label`, and the listed trade-side
  exemptions cite provider contracts.
- Task 630's pooled `connection_owner` reuses routed hosts; `owns_connection?`
  stays false on the routed path and remains a tested compatibility escape,
  not dead product surface.
- No leftover `IO.inspect` / `dbg` / bare TODO in the landed lib lines.
- Task 630's CHANGELOG entry already covers owned/reused/option-preserving
  split-host connections.

## Verification

- Cold `mix deps.get && mix check.dispatch` in this un-warmed worktree:
  **passed (exit 0)**. Offline suite **3,860 passed / 0 failed / 282
  excluded**. Oracle gate, authority check, error authority, lighter signer,
  `ccxt.claude_check`, `ccxt.agents_md --check`, `ex_dna` (0 clones), and
  `reach.check` all green.

## Discoveries filed

None. Every concrete finding was small enough to fix in this audit; no rmap
task was warranted.

## Reviewer-rejection note

The supplied recent rejection concerns task 551, which is not in this landed
range, so no false-rejection assessment applies.
