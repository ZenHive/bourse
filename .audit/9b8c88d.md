# Audit 9b8c88d

Reviewed `6065613^..9b8c88d`, concentrating on the authored-spec rotation, fixture/replay removal, task namespace rename, unified side validation, provider-live journey additions, their documentation, and the final reviewer fixes. I checked the touched runtime/test surfaces for dead names, stale paths, debug output, documentation drift, and convention violations.

## Findings and fixes

- Fixed three stale supported-command references in the Unreleased changelog after `mix ccxt.*` became `mix bourse.*`: `verify_rest_read_contracts`, `verify_ws_first_frame`, and `build_lighter_signer`. Historical references to tasks deleted under their old names remain unchanged.
- Cold dependency resolution reported published advisories against the locked transitive Gun/Cowlib HTTP stack. Filed task 683 to upgrade it and verify the live WebSocket surface; this audit does not fold a dependency upgrade into a documentation hygiene fix.
- No leftover debug output, orphaned runtime modules, or additional actionable naming/documentation drift was found in the landed implementation range.

## Reviewer feedback

Task 672's landed endpoint-major rotation is sound: all venue specs use the six-facet layout, the loader preserves strict reconstruction, and its structural tests and documentation survive the later range. The cited rejection was therefore a false rejection of salvageable work; the strictness issue was fixed inline before landing.

## Cold check

After `mix deps.get` populated the intentionally cold tree, `mix check.dispatch` completed compilation and static checks but exited red in the provider-live suite. The failures are explicit live-environment/state failures, chiefly missing Deribit/Derive/Binance USD-M credentials plus OKX demo-unavailable/account-state cases and an empty Lighter OHLCV response. This is recorded as a red post-merge witness, not treated as a landing gate.
