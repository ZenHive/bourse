# Audit 0ff5588

Reviewed `993351f^..0ff5588`, concentrating on the task 690 private-connect inventory test, the reviewer tightening of per-venue error reasons and the Binance futures position-mode live read, and the surrounding roadmap-only lifecycle commits (690 done then in_progress, 685 blocked, 687/688 in_progress). I checked the touched test surfaces for dead stubs, catch-all error paths, leftover debug, stale Hedge-Mode assumptions, CHANGELOG drift, and convention violations.

## Findings and fixes

- Clean. The agent delivery added an offline inventory that forbids an unauthenticated `{:ok, ws}` on every runtime venue's private connect; the reviewer replaced the catch-all `{:error, _}` with a per-venue expected reason derived from authored config (so a collapse to `:websocket_not_configured` cannot pass) and shared `maybe_put_position_side/2` across both Binance futures wallets so a hardcoded `positionSide` cannot go stale when one wallet is One-way and the other is Hedge. No dead code, leftover debug, inconsistent naming, or CHANGELOG gap: Unreleased already records the task 690 runtime fix from the prior audit of `f3bf06b`.
- The smoke-test comment that `fetchPositionMode` still fails open into `RawResponse` is accurate and already handled by a dual clause. That missing parse slice is an operator-routing question, not a new roadmap task. No audit discovery was filed. Cold `mix deps.get` repeated the Gun/Cowlib advisory output already tracked and blocked on task 683.

## Reviewer feedback

The supplied recent rejection concerns task 672, which is outside this landed range, so no false-rejection judgment applies here.

## Cold check

After `mix deps.get` populated the intentionally cold tree, `mix check.dispatch` completed compilation and static hygiene (format, compile --warnings-as-errors, credo --strict with no issues, doctor, sobelow) but exited red in the provider-live suite: 2,918 passed, 4 failed, 1 flaky (healed), 72 excluded. The confirmed failures were live provider/account-state conditions: empty Lighter candle and funding rows, a Binance USD-M trade window with only two distinct timestamps, and the matching Lighter OHLCV time-window case. This is recorded as a red post-merge witness and did not alter the settled merge.
