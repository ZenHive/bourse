# Post-merge audit — ec4784b

Range audited (already merged, never reverted):

- `aa3b358` harness: agent delivery — task 626 Deribit combo taxonomy,
  quantity semantics, and inverse classification
- `ec4784b` harness: reviewer fixes — tighten the type/flag reverse contract
  and leftover-combo detection

## Scope reviewed

- **Runtime contract** — `Bourse.Market.combo?/1`,
  `quantity_resolvable?/1`, Deribit combo type preservation, inverse-id
  classification, loaded-market reconciliation, and option-surface filtering.
- **Tests** — the recorded all-venue type/flag invariant, Deribit combo shape
  pins, inverse seed/corpus agreement, type helpers, and symbol round-trips.
- **Consumer records** — `[Unreleased]`, the Deribit carve register, and the
  W-612 entry in `BUGS.md`.
- **Hygiene** — dead/debug code, naming consistency, stale statements, and
  generated/project conventions in the touched surface.

## Findings

1. **Shared integration validator rejected the new market types (fixed).**
   Task 626 made `"future_combo"` and `"option_combo"` valid `Market.type`
   values, but `Bourse.StructValidators` still allowed only the prior type
   vocabulary. A live/integration `fetch_markets` validation would therefore
   reject the same Deribit rows the new offline contract accepts. Added both
   venue types and a focused regression test.

2. **CHANGELOG gap (fixed).** The breaking Deribit type change, the two new
   consumer helpers, and the inverse arithmetic correction were absent from
   `[Unreleased]`. Added entries under Added, Changed, and Fixed.

3. **BUGS W-612 still read as pending/folded (fixed).** The preserved sweep
   entry still described task 626 as future work. It now records the landed
   commits and the resolved classifier/type behavior while retaining the
   original repro.

## Checked and clean

- No added `IO.inspect`, `dbg`, logger/debug output, bare TODO/FIXME, or dead
  helper remained in the landed range.
- The historical C27 combo type sentence remains intentionally append-only;
  C-T626 explicitly supersedes only that sentence and preserves C27's native-id
  symbol contract.
- The reviewer additions pin unknown inverse shapes, recorded classifier
  agreement, and legacy `info.kind` combo detection. No false reviewer
  rejection was supplied for this range.

## Verification

- Focused: `mix test.json --quiet --output
  /tmp/audit-ec4784b-focused.json test/bourse/struct_validators_test.exs
  test/bourse/market_type_flag_contract_test.exs` — **55 passed / 0 failed**.
- Post-fix: `mix check.dispatch` with only the harness-injected `AGENTS.md`
  path-stashed, then restored unchanged — **passed**. Offline suite: **4,043
  passed / 0 failed / 288 excluded**. Oracle, authority/error checks, Lighter
  signer and parser coverage, CLAUDE/AGENTS checks, `ex_dna`, architecture,
  and smell checks all passed.

## Cold-build witness

The intentionally un-warmed `mix deps.get && mix check.dispatch` compiled the
dependencies and project, then finished red solely because harness had prepended
ephemeral instructions to `AGENTS.md`: the committed-tree freshness assertion
reported **4,041 passed / 1 failed / 288 excluded**. This was not a landed-range
compile or behavior failure. The required machine witness records the cold check
as failed; the post-fix committed-content gate above is green.

## Discoveries filed

None. All three hygiene findings were bounded and fixed in this audit.

## Reviewer-rejection note

No reviewer rejections were supplied for this project, so no false-rejection
assessment applies.
