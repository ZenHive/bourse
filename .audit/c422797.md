# Post-merge audit — c422797

Range audited (already merged, never reverted):

- `ed1268a` harness: agent delivery — task 544 alpaca/lighter public WebSocket transport
- `c422797` harness: reviewer fixes — regenerate AGENTS.md for the ten-venue WS claim

## Scope reviewed

- **Runtime code** — new `:action_key_secret` / `:action_channels` patterns, alpaca
  public `auth_sections`, lighter `:type_subscribe` transport, the
  `:websocket_not_configured` vs `:unsupported_exchange` split, SubscribeAck /
  AuthAck classifiers, and the Adapter leftover error path.
- **Authored specs and carves** — alpaca/lighter `websocket` slices, C-T544a/b,
  and the historical C-T451d absence claim.
- **Tests** — canaries, wiring, config partition, subscribe-ack, and facade
  error-atom tests.
- **Consumer and generated docs** — `[Unreleased]`, CLAUDE.md / AGENTS.md,
  README, BUGS.md, WS subscription table.

## Findings

1. **CHANGELOG gap (fixed).** Task 544 added consumer-visible public WS
   transport for alpaca and lighter and split the missing-config error atom.
   Neither was in `[Unreleased]`.

2. **C-T451d still read as current (fixed).** The 2026-07-23 Lighter carve
   said every WebSocket slot is `supported=false`. C-T544b already superseded
   that absence; the heading and a history bullet now say so.

3. **Adapter leftover used the old error atom (fixed).** `Bourse.WS.connect/3`
   distinguishes `:websocket_not_configured` from `:unsupported_exchange`, but
   `Bourse.WS.Adapter` still returned `:unsupported_exchange` for a runtime
   venue with no WS config. Both now share `Config.missing_config_error/1`.

4. **CLAUDE.md omitted Alpaca's public handshake (fixed).** `connect/3` now
   authenticates alpaca `:public` via `auth_sections`, but the doctrine
   paragraph only described private-section auth. Added the exception and
   regenerated `AGENTS.md`.

5. **Subscription pattern table omitted the runtime venue (fixed).**
   `:type_subscribe` still listed only kucoin/coinbaseexchange; lighter is the
   configured runtime user.

## Checked and clean

- No leftover `IO.inspect` / `dbg` / bare TODO, no debug output, no leaked
  harness scaffolding.
- README's generic WS example does not claim a venue count; BUGS.md has no
  stale 544 entry.
- The reviewer-only `AGENTS.md` regen matches the CLAUDE.md ten-venue claim.
  Coinbase Exchange remains the registered divergence.
- Facade, config-partition, auth-wiring, and subscribe-ack tests cover the
  new transport. Unified lighter `watch_*` templates stay unresolved on
  purpose (numeric market index).

## Verification

- Focused after the hygiene fixes: `mix test.json --quiet` on
  `test/bourse/ws/config_test.exs`, `test/bourse/ws/facade_test.exs`, and
  `test/bourse/carve_register_consistency_test.exs` — **36 passed / 0 failed**.
- Cold `mix deps.get && mix check.dispatch` compiled from an un-warmed tree.
  First pass failed `reach.check --smells --strict` on a trivial forwarder
  introduced by this audit (`WS.missing_config_error/1` wrapping
  `Config.missing_config_error/1`). Inlined the call. Second pass: **exit 0**.
  Offline suite **3,927 passed / 0 failed / 286 excluded**. Oracle gate, error
  authority, lighter signer, `ccxt.claude_check`, `ccxt.agents_md --check`,
  `ex_dna` (0 clones), and `reach.check --arch --smells --strict` all green.

## Cold-build witness

The intentionally un-warmed tree first reported missing locked dependencies.
After `mix deps.get`, `mix check.dispatch` compiled from cold. The first run
exited 1 on the trivial-forwarder smell above; that was an audit-introduced
finding, not a landed-range defect, and it is gone. The re-run passed.

## Discoveries filed

**Task 638** — Lighter's `subscribed/*` frame is both the subscribe
acknowledgement and the first snapshot. Default `WS.subscribe/3` consumes
that frame as `:success`, so the caller never sees the snapshot. The task-544
canary already works around this with `ack_timeout_ms: 0` on the success path.

## Reviewer-rejection note

No reviewer rejections were supplied for this project, so no false-rejection
assessment applies.
