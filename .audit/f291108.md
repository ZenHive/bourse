# Post-merge audit — f291108

Range audited (already merged, never reverted):

- `f291108` harness: agent delivery — task 648 Account class and margin
  model are not readable as unified facts (run
  run-1787146246444-a5bab029)

## Scope reviewed

- **Runtime** — `Bourse.fetch_account_facts/1` special-case in
  `Unified.call/5`, venue dispatch (`fetch_balance` vs account-info vs
  Binance family endpoints), and `ReadParse.account_facts/2` mappings
  for alpaca, binance, bybit, deribit, hyperliquid, and lighter.
- **Tests** — offline provider mappings, error envelopes, unmapped
  venues, Binance family selection without leaking the selector as a
  fact, and the tagged live integration pins.
- **Consumer records** — `[Unreleased]`, CLAUDE/AGENTS method count,
  the 2026-08-18 BUGS.md report, and carve register entries C-T648a–f.
- **Hygiene** — dead/debug code, naming consistency, leftover TODOs,
  and project conventions on the touched surface.

## Findings

1. **CHANGELOG gap (fixed).** The new public unified read was absent
   from `[Unreleased]`. Added an entry describing the three independent
   facts, the six mapped venues (including Binance spot / USD-M /
   COIN-M families on the umbrella client), and `:not_supported` for
   other runtime venues.

2. **BUGS.md still read as pending/triaged (fixed).** The 2026-08-18
   consumer report had only the workbench triage note for task 648. It
   now records the shipped read, carve ids, and two residuals: the
   derived callable surface does not advertise `fetchAccountFacts`
   (`has?/2` stays false because the method is special-cased outside
   authored `capabilities.has` / unified routes), and dedicated
   `binanceusdm` / `binancecoinm` / `okx` / `derive` clients still
   return `:not_supported`. Original repro text is unchanged.

3. **Stale unified method count (fixed).** CLAUDE.md / AGENTS.md still
   said 242 methods. `Unified.method_defs/0` has 246, including the
   newly added `fetch_account_facts`. Updated both files.

## Checked and clean

- No `IO.inspect`, `dbg`, logger/debug output, bare TODO/FIXME, or dead
  helper in the landed range.
- Carve registers C-T648a–f confront provider-owned fields and pin the
  live integration test as evidence; none cite CCXT as authority.
- Offline tests pin independent facts, unavailable-on-missing, and
  explicit `:not_supported` / exchange-error paths. Live tests stay
  behind `:integration` / `:network`.
- No false reviewer rejection was supplied for this range.

## Verification

- Cold-tree `mix deps.get && mix check.dispatch` — **passed**. Offline
  suite: **4,054 passed / 0 failed / 294 excluded**. Oracle, authority
  / error checks, Lighter signer and parser coverage, CLAUDE/AGENTS
  checks, `ex_dna`, architecture, and smell checks all passed.

## Discoveries filed

None. Residuals on `has?("fetchAccountFacts")` and the unmapped
dedicated futures / OKX / Derive clients are recorded on the original
BUGS.md entry. Authoring those into `capabilities.has` would re-pin
the capability surface; that is follow-up authoring, not a second
consumer report.

## Reviewer-rejection note

No reviewer rejections were supplied for this project, so no
false-rejection assessment applies.
