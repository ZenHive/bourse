# Post-merge audit — 8e59758

Range audited (already merged, never reverted):

- `9267848` harness: agent delivery — task 558 Deribit mutation adjudication
- `8e59758` harness: reviewer fixes — compensate when the acting observation fails

## Scope reviewed

- **Execution and validation code** — the mutation adjudication register validator,
  lifecycle executor, redaction helper, capture Mix task, oracle integration, and
  authored-operation comparator exposure.
- **Tests** — authorization refusals, lifecycle replay, cleanup compensation,
  credential handling, redaction, corpus tampering, and parameter-binding failures.
- **Evidence and docs** — the Deribit drift record, adjudication register, lifecycle
  plan, eight registered recordings, production verification ledger, and Sobelow
  suppressions.

## Findings

1. **Cleanup could be marked complete before it was judged (fixed).** The lifecycle
   set `cleaned?` as soon as a cleanup response arrived. If that response failed the
   reviewed assertion, compensation was skipped and the nested error path could claim
   no acting state had been placed. Cleanup is now marked complete only after its
   observation passes; an assertion mismatch retries the reviewed cancel and reports
   its outcome. Added a regression test that requires two cancel calls and rejects the
   stale "No compensating call was needed" message.

2. **Runtime naming leaked the roadmap task id (fixed).** New live captures would label
   orders `bourse-t558-*`, coupling durable runtime behavior to one implementation task.
   Renamed the default and test labels to the descriptive `bourse-mutation-*` form.
   Historical recordings remain unchanged as immutable evidence.

3. **CHANGELOG gap (fixed).** The substantial mutation-adjudication and reversible
   testnet capture surface was absent from `[Unreleased]`. Added an `Added` entry covering
   the reviewed safety boundary, redaction, lifecycle evidence, and oracle integration.

The lifecycle edit shifted two previously reviewed Sobelow file-write findings; their
line-sensitive fingerprints were regenerated with Sobelow and the stale fingerprints
were removed.

## Checked and clean

- No leftover debug output, bare TODO/FIXME markers, dead helper, or orphaned evidence
  path was found in the landed range.
- The committed register still partitions all 178 current-REST operations; the manifest
  and recordings remain hash-registered and pass the binary reality oracle.
- Public functions retain specs and module/function documentation, and the internal
  tooling remains excluded through the existing unpackaged oracle-provenance prefix.

## Verification

- Focused mutation-adjudication suite: **36 passed, 0 failed**.
- Focused coverage: `MutationAdjudication` **96.07%**, `Lifecycle` **95.33%**,
  `Redaction` **100%**.
- Post-fix `mix check.dispatch` against the committed `AGENTS.md`: **passed**,
  including oracle, authority, signer, AGENTS freshness, clone, architecture, and smell
  checks.
- Cold witness: the first invocation required the expected locked `mix deps.get`
  bootstrap; after bootstrap, `mix check.dispatch` reached the full suite but was
  **red only because the harness-injected worktree `AGENTS.md` differs from the committed
  generated file**. That pre-existing modification was preserved. Per the audit
  contract, this red cold fact did not revert or block the landed range.

## Discoveries filed

None. Every concrete issue found was small enough to fix inline; no separate rmap task
was warranted.

## Reviewer-rejection note

The supplied recent rejection concerns task 551, not task 558, so no false-rejection
assessment applies to this range.
