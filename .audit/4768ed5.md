# Post-merge audit — 4768ed5

Range audited (already merged, never reverted):

- `4768ed5` harness: agent delivery — task 634 Deribit shipped release 2.1.1 during a maintenance window: re-bind the current-REST corpus to the republished OpenAPI (run run-1787049218050-d21a82cf)

## Scope reviewed

- **Authority corpus** — Deribit `api-openapi` pin, contract baselines, current-REST
  drift report, mutation-adjudication denominator, and the provider-operation
  plan plus captured public proofs.
- **Tests** — authority-check, contract-compare, mutation-adjudication, and
  provider-operations pins for the 182-path inventory and the four added LSP
  reads.
- **Consumer docs** — CHANGELOG, BUGS.md, authored-specs dated measurement,
  and the production verification ledger.

## Findings

1. **Stale denominator count in live prose (fixed).** The landed rebind moved
   current REST from 178 to 182 operations and added the four LSP reads to
   task 557's bucket, but `Bourse.OracleProvenance.MutationAdjudication`'s
   `@moduledoc` and the Deribit WebSocket-session ledger entry still said
   "178-operation". Updated both to 182.

## Checked and clean

- Pins, baselines, the mutation register, the provider-operation plan, and the
  captured fixtures all quote SHA-256 `8dfbbe9f…` / ETag `W/"DXTaA468CADkchO3"`.
  Historical reports and `historical_current_rest` still describe the 178-path
  revisions they measured.
- The four added operations are private `account:read` LSP GETs, listed in
  `task_557_operation_keys`, with no removals. Mutation judgments and the
  reviewed public proof plan are unchanged.
- The dated 2026-08-04 measurement in `docs/authored-specs.md` remains a
  dated measurement, not a live gate.
- No CHANGELOG gap: task 634 rebinds provider evidence and does not change
  client behaviour.
- No leftover `IO.inspect` / `dbg` / bare TODO, no leaked harness scaffolding,
  no stale BUGS entries.

## Cold-build witness

Ran `mix deps.get && mix check.dispatch` in this intentionally un-warmed
worktree. Result: **passed (exit 0)**. Offline suite **3,878 passed / 0 failed /
282 excluded**. Oracle gate, error authority, lighter signer,
`ccxt.claude_check`, `ccxt.agents_md --check`, `ex_dna` (0 clones), and
`reach.check --arch --smells --strict` all green.

## Discoveries filed

None. The four new LSP reads already belong to pending tracker **557**; filing
a sibling would duplicate that inventory.

## Reviewer-rejection note

The supplied recent rejection concerns task 551, which is not in this landed
range, so no false-rejection assessment applies.
