# Post-merge audit — 4667e8f

Range audited (already merged, never reverted):

- `737c983` / `bc207e9` task 659 OrderPrecision snap_value non-numeric input
- `44257b4` task 660 Bybit dated-future padded DDMMMYY
- `031c231` / `bfd6b03` admission-rule docs and BUGS.md parking
- `7765406` req 0.7.3 / tidewave 0.9.0
- `9d32e7c` / `4667e8f` task 570 three-fact capabilities and labelled raw reads

## Scope reviewed

- **Runtime code** — `Bourse.RawResponse`, capability fact split
  (`has` / `mapping_complete` / `verification`), callable `has?/2` vs
  `venue_support/2`, parse-slot `unsupported_operation`, Bybit
  `pad_ddmmmyy_day/1`, `convert_date/3`, and OrderPrecision
  `invalid_parameters` for non-numeric snaps.
- **Authored specs and carves** — eleven runtime `mapping_complete` /
  `verification` maps, restored offered-but-unmapped cells, C-T570.
- **Tests and documentation** — unified-read contract, symbol round-trips,
  `[Unreleased]`, README, CLAUDE/AGENTS, BUGS.md parse_order_list.

## Findings

1. **`[Unreleased]` omitted the consumer-visible 570/659/660 contracts
   and still described the 658 convert_date pass-through (fixed).** Added
   RawResponse / three-fact / unsupported-slot entries, replaced the
   pass-through wording with the raise-and-pad contract, and recorded
   non-numeric precision snapping as `invalid_parameters`.

2. **README still called `has?/2` support introspection (fixed).** It now
   names the callable surface, `venue_support/2` for order-type flags, and
   `%Bourse.RawResponse{}` for incomplete mappings.

3. **CLAUDE.md / AGENTS.md still said incomplete reads return an
   unlabelled transport envelope (fixed).** Regenerated AGENTS.md. A raw
   parse slot the provider does not offer is now documented as
   `{:error, {:unsupported_operation, slot}}`.

4. **BUGS.md parse_order_list still ended on the pre-ship triage (fixed).**
   Appended the shipped 570 outcome: Deribit `parse_order_list/2` returns
   `unsupported_operation`, not `:no_field_map`.

5. **`convert_date/3` still passed unmatched `ddmmmyy` strings through
   after 660 documented a raise (fixed).** The leftover `_ -> date_str`
   arm now raises the same `ArgumentError` as unsupported pairs.

6. **`Bourse.RawResponse` was missing from the hexdocs Core API group
   (fixed).** Added it next to `Bourse.Parser`.

7. **New generated introspection had no test, and `__features__/0` still
   said "capability map" (fixed).** `__venue_support__/0` is pinned equal
   to `__features__/0`; mapping-complete and verification keys match
   `endpoints.unified`.

8. **Touched `Parser.parse/4` docstring still said "upstream Phase 12
   field maps" (fixed).** Reworded to authored field maps.

## Checked and clean

- No leftover `IO.inspect`, `dbg`, bare TODO/FIXME, or debug prints in
  the landed range.
- Order-type flags remain on provider `capabilities.has`; `exchange.has`
  is the callable boolean map. Reviewer-fix tests pin that split.
- `__venue_support__/0` is an alias of `__features__/0` (provider
  support), documented as such rather than deleted.
- Six-character non-numeric `:yymmdd` still raises from
  `String.to_integer/1`, and a regex-matching unknown month still raises
  `KeyError` from `Map.fetch!/2`. Those are still loud failures, not
  silent pass-through, so no separate rmap task.
- The 17 restored 570 cells stay offered, incomplete, unverified, and
  callable as labelled raw reads.
- Dep bump is req 0.7.3 / tidewave 0.9.0 only; no consumer API change.

## Verification

- `mix test.json --quiet --output /tmp/audit-4667e8f-focused.json
  test/bourse/symbol_test.exs test/bourse/exchange_generator_test.exs
  test/bourse/unified/order_precision_test.exs
  test/bourse/exchange_test.exs`: **362 passed / 0 failed**.
- `mix ccxt.agents_md --check`: **OK**.

## Cold-build witness

Ran `mix deps.get && mix check.dispatch` in this intentionally un-warmed
worktree (no copied `deps/` or `_build/` at start). First compile built
deps and the project. Dispatch gate **passed**. Offline suite **4,035
passed / 0 failed / 288 excluded**. Oracle gate, authority check, error
authority, `ccxt.claude_check`, `ccxt.agents_md --check`,
`ex_dna --max-clones 0`, and `reach.check --arch --smells --strict` all
passed. This post-merge fact did not revert, unmerge, or block the
settled range.

## Discoveries filed

None. Every concrete hygiene issue was small enough to fix here.

## Reviewer-rejection note

No reviewer rejections were supplied for this project, so no
false-rejection assessment applies.
