# Post-merge audit — 884af46

Range audited (already merged, never reverted):

- `884af46` test(markets): pin field-recipe schema and recording-less venue sweep
- `4f3c41e` harness: agent delivery — task 623 binanceusdm linear contract unit
- `94cf5d6` harness: reviewer fixes — task 569 signature-neutral bump + signer docs
- `074bdd7` harness: agent delivery — task 569 Lighter C-exposed signing vectors
- `396afb1` docs(changelog): label the deribit position unit change Breaking
- `956e5e4` chore(release): 0.5.0
- `310da56` chore(deps): patch bumps — descripex 0.12.1, tidewave 0.8.4, zen_websocket 0.6.1
- `8658b92` BUGS: constrain the re-signing report
- `2c4c942` docs(bugs): triage the three open entries — tasks 621/622/623 filed
- `24eca40` BUGS: signed requests are retried without re-signing
- `5752a26` BUGS: deribit client_order_id round-trip
- `d65a93f` refactor(arc): extract shared PathGuard

## Scope reviewed

- **Library code** — `Bourse.Unified.ContractUnit`, the `markets.contract_unit`
  schema/exchange-config wiring, the `read_parse` hook, `Bourse.Market` field
  docs, and the PathGuard extraction from three oracle-provenance copies.
- **Native signer** — `native/lighter_signer/golden_test.go` vectors for the
  five previously shape-only C-exposed operations, plus the README verification
  boundary and the signature-neutral bump note.
- **Docs / consumer queue** — CHANGELOG 0.5.0 cut and Unreleased, the binanceusdm
  C-T623a carve, BUGS.md entries for tasks 621/622/623, the re-signing causality
  constraint, and the ledger prose-set honesty tweak.
- **Tests** — field-recipe schema pin, recording-less venue sweep, recorded
  USD-M/COIN-M unit split, and the live fapi/dapi integration probe (network-tagged).

## Findings

1. **CHANGELOG gap (fixed).** Task 623 landed after the 0.5.0 cut and changed a
   consumer-visible market field: binanceusdm linear `contract_size` is now `1`
   with `quantity_unit: "base"` instead of `nil`. `[Unreleased]` was empty. Added
   a `Fixed` entry.

2. **Stale BUGS status (fixed).** The 2026-08-17 binanceusdm `fetchMarkets`
   entry still read 🔀 triaged after 4f3c41e shipped the fix. Header now says
   ✅ fixed, keeps the original triage as the evidence trail, and points the
   residual known-gap venues at filed task 625.

## Checked and clean (no action)

- **No dead code / leftover debug.** PathGuard is the single traversal guard;
  the three local copies are gone. ContractUnit has no `IO.inspect`/`dbg`/`TODO`.
  `oracle_provenance` remains unpackaged.
- **Task 569 is test-only.** The new golden vectors and README boundary update
  do not change runtime signing; no CHANGELOG entry is warranted. The reviewer
  fix correctly recorded the go-ethereum/gnark-crypto bump as signature-neutral.
- **0.5.0 cut is coherent.** mix.exs is `0.5.0`; the deribit position-unit
  Breaking label and upgrade note (396afb1) sit inside that section.
- **Known-gap sweep is honest.** `binance`, `bybit`, and `derive` stay named
  rather than silently nil; coinbaseexchange is listed as recording-less. The
  allowlist is a ratchet, not a silent default of 1.
- **BUGS newest-first is already drifted** (German 2026-08-10/14 reports sit
  after June 2026 English entries). This range appended at the bottom in that
  existing pattern; a wholesale reorder was out of scope.

## Cold-build witness

Ran `mix deps.get && mix check.dispatch` in this intentionally un-warmed
worktree. Result: **passed (exit 0)** — precommit, `ccxt.oracle_gate` ("all
modules verified", "binary oracle exact-set ratchet passed"), lighter signer
build + parser coverage 95.78%, `ccxt.claude_check` ("mechanical claims match
the tree"), `ccxt.agents_md --check` ("AGENTS.md is up to date"), `ex_dna`
(no duplication), and `reach.check` (architecture + smells OK) all green.

## Discoveries filed

- **task 625** — author remaining linear `contract_unit` recipes for the three
  known-gap venues (`binance` umbrella, `bybit`, `derive`). Machinery is already
  on main from task 623; the units themselves are still unauthored.

## Reviewer-rejection note

The supplied recent rejection concerns task 551, which is not in this range,
so no false-rejection assessment applies.
