# Post-merge audit — 9701bdf

Range audited (already merged, never reverted):

- `332e32a` re-pin the republished Deribit current AsyncAPI
- `50477a7` make the concurrent default-family assertion order-independent
- `5295fdd` task 628 Binance partial-depth Broadcast routing
- `fa62fc7` / `0ec9655` task 635 inclusive OKX cursor compensation
- `daa9825` / `b701884` task 616 plural-read shape enforcement and fixture coverage
- `a6e4f38` / `680a1ab` task 585 client-side transfer and leverage filters
- `1614d01` / `1530ff8` task 562 response-envelope field sources
- `fca45b5` / `9701bdf` task 636 retained authority surface digests

## Scope reviewed

- **Runtime and internal tooling** — WebSocket envelope routing, unified request
  and response shaping, field-source schema validation, response recording, and
  authority contract normalization/diffing.
- **Tests and evidence** — focused unit/integration tests, registered response
  fixtures, the Deribit drift report and retained digest, and authority corpus
  baselines/manifests.
- **Consumer and authoring docs** — `BUGS.md`, `[Unreleased]` in
  `CHANGELOG.md`, authored carve registers, `docs/authored-specs.md`, and the
  authority retention policy.

## Findings

1. **Untyped Postman data escaped the typed-retention boundary (fixed).** The
   task-636 policy and `ContractSource.surface_digest/2` documentation say only
   typed OpenAPI/AsyncAPI artifacts retain structural identities, but the
   implementation reused normalized Postman operations and emitted operation
   keys and entity hashes. Postman parsing remains available for contract
   comparison; surface digests now retain identities only for OpenAPI/AsyncAPI.
   Added a regression asserting that an untyped Postman collection produces
   empty key sets and entities.

2. **Consumer CHANGELOG coverage was incomplete (fixed).** Tasks 562, 585, 616,
   and 628 had no `[Unreleased]` entries, and task 635 mentioned candles but not
   the deposit-history and positions-history sites fixed in the same delivery.
   Added the missing behavior and expanded the OKX entry, including caller-wins
   native cursor behavior. The authority re-pin/digest work and test-only
   stability commits are internal maintenance and do not need consumer entries.

3. **Consumer bug statuses were stale or ambiguous (fixed).** The Bybit ticker
   clock report still marked task 562 open after both delivery and reviewer-fix
   commits landed. The task-628 residual described the repaired routing without
   explicitly saying it was fixed. Updated both while preserving their original
   repro/evidence trails.

## Checked and clean

- No new debug output, bare TODO/FIXME marker, dead helper, whitespace error, or
  orphaned evidence path was found in the landed range.
- The Deribit retained digest remains bound to the manifest pin and names the
  three previously unnameable channels. Offline authority validation covers all
  eleven venues.
- The WebSocket routing, inclusive cursor, client-side filter, plural-shape, and
  envelope-clock changes have focused positive and negative tests. The
  task-636 modules remain repo-internal Mix tooling and do not enter the Hex
  package.

## Verification

- Pre-mutation offline coverage: 3,910 passed / 1 failed / 283 excluded. The
  sole failure was the harness-injected `AGENTS.md` freshness assertion.
  `Mix.Tasks.Ccxt.ContractSource` was 85.50%,
  `Mix.Tasks.Ccxt.ContractComparator` 93.77%, and
  `Mix.Tasks.Ccxt.AuthorityCorpus` 97.79%.
- Focused contract-comparator suite after the fix: **21 passed, 0 failed**.
- `mix ccxt.authority_check`: **passed**, 11 venues validated offline.
- Post-fix `mix check.dispatch`: 3,910 passed / 1 failed / 283 excluded; the
  only failure remained the ephemeral harness `AGENTS.md` preamble making the
  working file differ from the committed generated file. The injected file was
  preserved and excluded from this audit commit.

## Cold-build witness

The intentionally un-warmed tree first reported the expected missing locked
dependencies. After `mix deps.get`, `mix check.dispatch` compiled from cold and
reached the offline suite: **3,910 passed / 1 failed / 283 excluded**. The sole
failure was `test/mix/tasks/ccxt_agents_md_test.exs:407`, caused by the
harness-injected `AGENTS.md` preamble. This red post-merge fact did not revert,
unmerge, or block the settled range.

## Discoveries filed

None. No new follow-up warranted an rmap task; the already-pending task 637
separately tracks the known OKX withdrawal-cursor site outside this landed
range.

## Reviewer-rejection note

No reviewer rejections were supplied for this project, so no false-rejection
assessment applies.
