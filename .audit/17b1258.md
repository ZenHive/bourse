# Post-merge audit — 17b1258

Range audited (already merged, never reverted):

- `17b1258` / `b27ce92` task 632 Binance-family order-type read/write parity
- `f781dfa` task 631 mixed-host WebSocket rollback and connection ownership
- `2705796` authority drift review and Deribit contract re-pins
- `3a79b2d` client-order-id native-key round-trip invariant

## Scope reviewed

- **Runtime code** — Binance product-scoped order-type annotation and
  split-host WebSocket subscription/connection ownership.
- **Tests and invariants** — Binance order-type inventory, WebSocket rollback
  and owner lifecycle, and client-order-id native-key parity.
- **Authority evidence** — Binance/Coinbase/Deribit/Derive manifest changes and
  Deribit's contract-comparator baselines.
- **Consumer docs** — `[Unreleased]`, WS module/config inventories, and the
  three Binance-family carve registers.

## Findings

1. **CHANGELOG gap (fixed).** Tasks 631 and 632 changed consumer-visible
   behavior but were absent from `[Unreleased]`. Added the mixed-host rollback
   guarantee and the product-scoped conditional order-type round trip.

2. **Stale WS inventory prose (fixed).** `Bourse.WS` and `Bourse.WS.Config`
   still claimed seven configured venues after binancecoinm became the eighth
   hand base. Updated both inventories and described the scope as transport
   configuration, since binancecoinm's unified channel templates remain a
   separate authored capability.

3. **Inconsistent test naming (fixed).** The new
   `BinanceOrderTypeRoundtripTest` / `binance_order_type_roundtrip_test.exs`
   conflicted with the repository's established `RoundTrip` naming. Renamed
   the module/file and updated the carve evidence paths.

4. **Stale Deribit contract baselines (fixed).** `2705796` re-pinned upcoming
   REST plus current/upcoming AsyncAPI hashes in `manifest.json` without
   rebinding `contract-baselines.json`. The cold suite found two comparator
   failures. Bound all three baseline SHA-256 values to the landed manifest;
   the focused comparator suite is now 16 passed / 0 failed.

## Checked and clean

- No dead code, leftover debug output, bare TODO/FIXME, disabled checks, or
  whitespace errors were introduced by the range.
- The Binance type tables are product-scoped and reject unknown native values;
  list parsing propagates the first unmapped type rather than partially
  returning downcased data.
- Mixed-host success still reuses one connection per host. Failure rolls back
  prior groups, and the linked connection owner closes held clients on normal
  stop or abnormal exit.
- The client-order-id invariant now verifies that authored request keys overlap
  the order/trade response keys. Elixir `RequestShape` mappings remain an
  explicit inventory because their native key is not represented in the JSON
  spec; no concrete mismatch was found.

## Cold-build witness

Ran `mix deps.get && mix check.dispatch` in this intentionally un-warmed
worktree. Result: **failed** — offline suite **3,872 passed / 3 failed / 282
excluded**.

- One failure is the expected harness-only `AGENTS.md` freshness mismatch: the
  worktree contains the required ephemeral harness header, which this audit
  intentionally does not commit.
- Two failures were the Deribit baseline mismatch in finding 4. After the fix,
  `mix test.json test/mix/tasks/ccxt_contract_compare_test.exs --quiet` passed
  **16 / 16**. The original cold witness remains recorded as red; it was not
  rerun or rewritten.

## Discoveries filed

None. Every concrete finding was local and fixed in this audit; no rmap task
was warranted.

## Reviewer-rejection note

The supplied recent rejection concerns task 551, which is not in this landed
range, so no false-rejection assessment applies.
