# Post-merge audit — fe7d208

Range audited (already merged, never reverted):

- `91fe347` — BUGS.md: atom-side / dropped-params report marked fixed by task 663
- `4fff27f` — RequestShape keeps caller native keys; schema forbids silent side defaults
- `f288588` / `6cbc33b` / `6346f4f` — HMAC leftover executors dropped; `sign_recipe` required
- `29c458d` / `57284d8` — live-call-first verification doctrine
- `1d653c7` — task 667 provider-live REST-read contracts
- `fe7d208` — bound those contracts to venue-native sandbox surfaces (427 branches)

## Scope reviewed

- **Runtime** — HMAC dispatch (`Bourse.Signing` / `HmacRecipe`), RequestShape native-key
  preservation, schema `endpoint_selection` guard, live-lane corpus tags.
- **Operator surface** — `mix ccxt.verify_rest_read_contracts`,
  `priv/authority/rest-read-contracts.json`, `ops/live-drift.sh`.
- **Docs** — CHANGELOG `[Unreleased]`, CLAUDE/AGENTS running-tests region,
  BUGS.md 663 entry, prod-verification ledger, COVERAGE.md, authored-specs.md.
- **Hygiene** — dead modules/flags/tags, leftover debug, stale counts, naming.

## Findings

1. **`[Unreleased]` omitted the three landed consumer/operator changes (fixed).**
   Recorded the REST-read live lane, HMAC `sign_recipe` requirement / executor
   deletion, native-key preservation, and the direction-bearing selection guard.

2. **Dead live-lane tags after the REST-read replacement (fixed).**
   Task 667 deleted the `:capability_live`, `:public_probe`, and
   `:symbol_public_probe` suites. The weekly corpus still included/excluded
   those tags, `test_helper.exs` still default-excluded `:capability_live`, and
   CLAUDE.md still listed it. Dropped the unused tags. Also moved the HMAC
   telemetry comment onto `timed_sign/6`, which is the wrapper it describes.

3. **Dead `preserve_native` flag in the Deribit authored spec (left in place).**
   `4fff27f` made native-key preservation the default and deleted
   `preserve_native?/1`. `createOrder.label` still carries `"preserve_native":
   true`, which the runtime no longer reads. Removing it changes the authored-spec
   canonical SHA-256 pinned by `test/fixtures/provider_operations/_manifest.json`,
   so the no-op field stays rather than retouch the capture inventory.

## Checked and clean

- HMAC executor modules (`HmacSha256Query` / `Headers` / `Iso`) have no remaining
  references. `module_for_pattern/1` and the three HMAC `sign/4` heads route
  through `HmacRecipe`.
- Inventory denominator is 427 everywhere it is claimed (CLAUDE/AGENTS, inventory
  test, mix-task tests). `fe7d208` already corrected the 901 → 427 drift.
- No landed `IO.inspect`, `dbg`, bare TODO/FIXME, silently passing assertion, or
  generated-file hand edit.
- BUGS.md 663 entry correctly names the still-open batch-write nested-side path
  as separately filed. Sandbox-unhosted REST-read product surfaces are already
  on the prod-verification ledger under task 667.
- No reviewer rejections were supplied for this project.

## Verification

- Focused: `mix test.json --quiet --output /tmp/audit-fe7d208-focused.json`
  `test/bourse/live_lane_test.exs test/bourse/signing_test.exs
  test/bourse/spec_test.exs test/bourse/unified/request_shape_test.exs
  test/bourse/rest_read_contract_inventory_test.exs
  test/mix/tasks/ccxt_verify_rest_read_contracts_test.exs` — **313 passed /
  0 failed**.
- `mix ccxt.agents_md --check` and `mix ccxt.claude_check` — **passed**.
- Post-fix `mix check.dispatch`: **passed**. Offline suite **3,988 passed /
  0 failed / 697 excluded**.

## Cold-build witness

Ran `mix deps.get && mix check.dispatch` in the intentionally un-warmed
worktree (no copied `deps/` or `_build/` at start). First compile built deps
and the project; the dispatch gate **passed**: offline suite **3,988 passed /
0 failed / 697 excluded**. Oracle gate, authority check, error authority,
Lighter signer, CLAUDE/AGENTS checks, `ex_dna --max-clones 0`, and
`reach.check --arch --smells --strict` all passed. This post-merge fact did
not revert, unmerge, or block the settled range.

## Discoveries filed

None. Actionable leftovers were small enough to fix here. The sandbox-unhosted
REST-read surfaces and the nested batch-write `side` path are already tracked
(prod-verification ledger / BUGS.md).

## Reviewer-rejection note

No reviewer rejections were supplied for this project, so no false-rejection
assessment applies.
