# Post-merge audit — 74ca5d2

Range audited (already merged, never reverted):

- `a86abb0` / `2e25502` — task 572 Bybit category contracts and reviewer fixes
- `df49757` / `9e0b9cf` / `c4b528d` / `d162044` — bug-ledger and triage updates
- `a07bd84` / `75bd30f` — dependency updates
- `848ebb3` / `fda5fa7` — task 662 request-option classification and reviewer fixes
- `6e86807` — 0.7.0 release preparation
- `74ca5d2` — Deribit option premium notional derivation

## Scope reviewed

- **Runtime and authored contracts** — Bybit category resolution, HTTP option
  validation/circuit-breaker classification, `load_markets/2` option handling,
  and Deribit option position units.
- **Tests and evidence** — offline request-shape, HTTP, unified-read, position-unit,
  authored-spec, recorded-response, and tagged live integration additions.
- **Dependencies** — locked Bandit 1.12.5, ExUnitJSON 0.6.1, ZenWebsocket 0.7.1,
  and Descripex 0.13.0 against their official HexDocs APIs/release notes and the
  repository call sites.
- **Release/consumer records** — `CHANGELOG.md`, `BUGS.md`, README version guidance,
  carve registers, generated-spec conventions, and commit naming.
- **Hygiene** — dead/debug code, hidden test outcomes, stale statements, naming,
  and formatting in the touched surface.

## Findings

1. **Missing post-release changelog section (fixed).** `74ca5d2` landed after the
   `v0.7.0` tag but `CHANGELOG.md` still ended at 0.7.0. Restored `[Unreleased]`
   and recorded the Deribit option premium-notional behavior without attributing
   it to the already-published release.

2. **Task 664 bug entry still read as triaged (fixed).** Updated the status to
   fixed on `main` after 0.7.0 and appended the landed behavior/evidence while
   preserving the original consumer report.

3. **Descripex option metadata omitted two accepted aliases (fixed).** Runtime
   code, prose, and tests accepted `:type`, `:subType`, and `:sub_type`, but
   `Bourse.__api__(:load_markets)` advertised only `:type`. Added both aliases to
   the declaration and a focused introspection regression test.

4. **Historical commit subject violated repository convention (not rewritten).**
   `75bd30f` is titled `descripes updated`, which is misspelled and lacks a
   conventional-commit type/scope. The merge is settled, so the audit does not
   rewrite published history; no source or follow-up task is needed.

## Checked and clean

- No landed `IO.inspect`, `dbg`, new logger/debug output, bare TODO/FIXME, dead
  helper, silently passing assertion, or generated-file hand edit was found.
- Bandit retains the used `start_link/1` surface; ExUnitJSON retains
  `Trace.setup/1`; ZenWebsocket retains the used `Client.connect/2`,
  `send_message/2`, `subscribe/2`, `close/1`, and state/health functions. The
  removed ZenWebsocket 0.7.0 surfaces are not referenced by bourse.
- The dependency resolution is coherent: ZenWebsocket 0.7.1 intentionally
  widens its Descripex bound and the lock resolves Descripex 0.13.0.
- Bybit and Deribit carve entries remain provider-authoritative and carry
  registered reality evidence. No false reviewer rejection was supplied.

## Verification

- Pre-edit coverage: `Bourse` **91.3%** (21 covered lines; uncovered 204/206),
  above the 80% standard-module gate. The coverage run's sole test failure was
  the known harness-injected `AGENTS.md` freshness mismatch; the project gates
  below used the committed generated file.
- Focused: `mix test.json test/bourse_test.exs --quiet --output
  /tmp/audit-74ca5d2-focused.json` — **34 passed / 0 failed**.
- Final: `mix check.dispatch` with only the harness-injected `AGENTS.md` banner
  path-stashed, then restored unchanged — **passed**. Offline suite: **4,078
  passed / 0 failed / 297 excluded**. Oracle, authority/error checks, Lighter
  signer/parser coverage, CLAUDE/AGENTS checks, duplication, architecture, and
  smell checks passed.

## Cold-build witness

The intentionally un-warmed tree ran `mix deps.get` followed by
`mix check.dispatch` with the ephemeral `AGENTS.md` banner path-stashed. It
compiled the locked dependency set and project, then **passed**: **4,077 passed /
0 failed / 297 excluded**, with all remaining dispatch checks green.

## Discoveries filed

None. All actionable findings were bounded and fixed in this audit; the
historical commit subject does not justify roadmap work.

## Reviewer-rejection note

No reviewer rejections were supplied for this project, so no false-rejection
assessment applies.
