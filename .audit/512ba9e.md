# Audit 512ba9e

Reviewed `10343c9^..512ba9e`, concentrating on the five provider-live trader journeys, credential-registry isolation, option-position notional normalization, Lighter account-scoped reads, and the Lighter ChangePubKey signer/provisioning path. I checked the touched runtime, native, test, and documentation surfaces for dead code, stale names, debug output, convention violations, and release-note drift.

## Findings and fixes

- Added the missing Unreleased changelog entry for `mix bourse.provision_lighter`, including its key-custody boundary and end-to-end verification behavior.
- Corrected the stale Deribit-only option-notional changelog entry. It now records the landed common premium-value meaning and the OKX/Bybit provider fields as well as Deribit's derivation.
- No dead code, leftover debug output, inconsistent naming, or further actionable documentation drift was found.
- Cold dependency resolution repeated the advisory output already adjudicated and tracked by task 683; no duplicate task was filed. No new audit discoveries required filing.

## Reviewer feedback

The supplied recent rejection concerns task 672, which is outside this landed range, so no false-rejection judgment applies here.

## Cold check

After `mix deps.get` populated the intentionally cold tree, `mix check.dispatch` completed compilation and static hygiene checks but exited red in the provider-live suite: 2,775 passed, 59 failed, 41 were invalid after a setup failure, and one failure healed as flaky. The failures were live provider/account-state conditions, including Deribit HTTP 502s, absent open-order state, a Binance COIN-M balance below the pinned minimum, and empty Lighter OHLCV rows. This is recorded as a red post-merge witness and did not alter the settled merge.
