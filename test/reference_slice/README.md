# Reference slice

CCXT-derived reference documents — authoring/test input only. Never loaded at
runtime, never packaged. The 16-document inventory is pinned by
`reference_corpus.json` and validated by `Bourse.ReferenceSlice`
(`test/support/reference_slice.ex`).
