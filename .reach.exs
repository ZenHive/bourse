# Reach architecture/smell policy (vibe_kit baseline).
# `[]` is the valid no-policy default — `reach.check --arch --smells` runs the
# built-in smell/architecture analysis with no custom layer or boundary rules.
# Populate with layer/boundary policies as the architecture solidifies.
[]
